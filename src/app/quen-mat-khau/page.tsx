"use client";import{useState}from"react";import Link from"next/link";import{SiteHeader}from"@/components/layout/SiteHeader";import{SiteFooter}from"@/components/layout/SiteFooter";import{GlassPanel}from"@/components/ui/GlassPanel";import{Input}from"@/components/ui/Input";import{Button}from"@/components/ui/Button";export default function Page(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);const submit=async()=>{setBusy(true);try{const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const b=await r.json().catch(()=>({}));setMsg(b.message||'Nếu email tồn tại, hướng dẫn khôi phục đã được gửi.')}catch{setMsg('Không thể kết nối máy chủ.')}finally{setBusy(false)}};return <div className="min-h-screen bg-bg-primary"><SiteHeader/><main className="flex min-h-[70vh] items-center justify-center px-4 py-12"><GlassPanel className="w-full max-w-md p-7 sm:p-9"><p className="text-[10px] font-bold uppercase tracking-[.2em] text-accent-orange">Account recovery</p><h1 className="mt-3 text-h2 font-display text-white">Quên mật khẩu?</h1><p className="mt-3 text-small text-white/50">Nhập email đăng ký để nhận hướng dẫn.</p><div className="mt-6"><Input label="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div>{msg&&<p className="mt-4 text-small text-state-success">{msg}</p>}<Button className="mt-5 w-full" onClick={submit} isLoading={busy}>Gửi hướng dẫn</Button><Link href="/dang-nhap" className="mt-5 block text-center text-small text-white/45">← Quay lại đăng nhập</Link></GlassPanel></main><SiteFooter/></div>}